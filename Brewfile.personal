# Brewfile.personal - Full personal Mac setup
# Only run on your own machines
#
# Note: CLI tools (bat, fd, rg, jq, starship, zoxide, neovim) managed by mise
# Note: GUI apps that auto-update (Spotify, Signal, Zed, etc.) installed manually

# Development
brew "gh"
brew "jj"
brew "colima"  # includes docker CLI
brew "postgresql@14"

# Infrastructure
brew "ansible"

# Tools
brew "tmux"
brew "ollama"

# Window management
tap "nikitabobko/tap"
cask "aerospace"
tap "felixkratz/formulae"
brew "borders"

# Automation & keyboard
cask "hammerspoon"
cask "keymapp"

# Terminal
cask "wezterm"
